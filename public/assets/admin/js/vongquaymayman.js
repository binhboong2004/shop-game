document.addEventListener('DOMContentLoaded', function() {
    const btnAddWheel = document.getElementById('btnAddWheel');
    
    if (btnAddWheel) {
        btnAddWheel.addEventListener('click', function() {
            alert('Tính năng "Thêm Vòng Quay Mới" đang được phát triển!');
        });
    }
});
